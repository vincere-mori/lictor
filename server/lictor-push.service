[Unit]
Description=Lictor push backend
After=network.target

[Service]
WorkingDirectory=/opt/lictor-push
EnvironmentFile=/opt/lictor-push/.env
ExecStart=/opt/lictor-push/.venv/bin/uvicorn app:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
